package linkedlist;

public class MiddleOfList {
    /*
    class Node1{
        int data;
        Merge2SortedList.Node next;
        Node1(int new_data){
            data=new_data;
            next=null;
        }
    }
    public Node1 middle(Node1 head){
        Node1 slow=head;
        Node1 fast=head;
        while (fast && fast.next){
           slow= slow.next;
          fast=fast.next.next;
        }
        return slow;
    }

     */
}
