package stack_quea;

import java.util.Stack;

import static java.lang.Math.floor;
/*
public class DeleteMiddleeleinStack {
    void deletemiddle(Stack<Character> st, int n, int cur=0){
        if (st.empty()||cur==n)
            return;
        int x=st.top();
        st.pop();
        deletemiddle(st,n,cur++);
        if (cur!=floor(n/2)+1)
            st.push(x);

    }
}*/
